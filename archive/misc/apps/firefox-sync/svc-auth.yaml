---
apiVersion: v1
kind: Service
metadata:
  name: ff-sync-auth-svc
  namespace: apps
spec:
  ports:
    - protocol: TCP
      name: web
      port: 80
      targetPort: 5000
  selector:
    app: ff-sync-auth
