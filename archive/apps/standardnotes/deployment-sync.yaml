---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: sn-sync
  namespace: apps
  labels:
    app: sn-sync
    group: standardnotes
    purpose: manager
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sn-sync
  template:
    metadata:
      labels:
        app: sn-sync
        group: standardnotes
        purpose: manager
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: "kubernetes.io/arch"
                operator: In
                values: ["amd64"]
      containers:
      - name: sn-sync-server
        image: standardnotes/syncing-server-js:1.51.0 # {"$imagepolicy": "flux-system:sn-sync"}
        imagePullPolicy: Always
        command:
        - ./docker/entrypoint.sh
        args:
        - start-web
        envFrom:
        - configMapRef:
            name: sn-default-env
        - secretRef:
            name: sn-secret
        livenessProbe:
          httpGet:
            path: /healthcheck
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 15
        resources:
          limits:
            cpu: "100m"
            memory: "128Mi"
          requests:
            cpu: "25m"
            memory: "32Mi"
      dnsPolicy: ClusterFirst
      restartPolicy: Always
